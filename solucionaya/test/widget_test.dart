import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Dummy test for Day 2', (WidgetTester tester) async {
    // La prueba de widgets completa se realizará cuando se inyecten
    // los repositorios de Firebase mockeados (Day 9+).
    expect(true, isTrue);
  });
}
